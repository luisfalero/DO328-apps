apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: preference
  namespace: resilience-retry
spec:
  hosts:
    - preference
  http:
    - route:
        - destination:
            host: preference
      retries:
        attempts: 100
        perTryTimeout: 2s
        retryOn: 5xx
